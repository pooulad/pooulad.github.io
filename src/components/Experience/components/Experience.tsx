import React, { memo } from "react";

function Experience() {
  return (
    <div>
      <div>Experience</div>
      <div>Experience</div>
      <div>Experience</div>
    </div>
  );
}

export default memo(Experience);
